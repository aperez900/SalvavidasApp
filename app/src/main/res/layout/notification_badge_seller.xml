<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="?attr/actionBarSize"
    android:layout_height="?attr/actionBarSize">

    <ImageView
        android:id="@+id/img_notificacion_productos"
        android:layout_width="@dimen/notification_icon_size"
        android:layout_height="@dimen/notification_icon_size"
        android:src="@drawable/icono_notifications"
        android:layout_gravity="center"
        android:contentDescription="@string/descripcionImagen" />

    <TextView
        android:id="@+id/tv_nro_productos_solicitados"
        android:layout_width="@dimen/badge_size"
        android:layout_height="@dimen/badge_size"
        android:background="@drawable/badge"
        android:text="2"
        android:textSize="10sp"
        android:textStyle="bold"
        android:textColor="@color/newRed"
        android:layout_gravity="center"
        android:gravity="center"
        android:textAlignment="center"
        android:layout_marginStart="10dp"
        android:layout_marginBottom="10dp"/>
</FrameLayout>